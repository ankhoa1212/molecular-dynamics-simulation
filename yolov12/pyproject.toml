[project]
name = "yolov12"
version = "0.2.0"
description = "machine learning for particle tracking"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mlflow>=2.0.0",
    "pyyaml>=6.0",
    "ultralytics>=8.3.0",
]
